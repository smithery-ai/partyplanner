# Hylo Backend Node

Node/Hono implementation of the Hylo backend API, backed by PGlite.

From this directory:

```sh
pnpm dev
```

Schema tasks are package-owned:

```sh
pnpm db:migrate
pnpm db:studio
```
