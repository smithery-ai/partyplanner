{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "portless run --force --name api-worker.hylo sh -c 'wrangler dev --ip 127.0.0.1 --port \"${PORT:-8788}\"'",
    "db:migrate": "HYLO_DB_APP=backend pnpm -C ../../packages/postgres db:migrate",
    "db:studio": "HYLO_DB_APP=backend pnpm -C ../../packages/postgres db:studio",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@workflow/core": "workspace:*",
    "@workflow/remote": "workspace:*",
    "@workflow/runtime": "workspace:*",
    "@workflow/server": "workspace:*",
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^4.0.0",
    "typescript": "~6.0.2"
  }
}
