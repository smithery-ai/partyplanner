{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": [
    "HYLO_BACKEND_NODE_DATA_DIR",
    "HYLO_BACKEND_URL",
    "PORTLESS",
    "PORTLESS_*",
    "VITE_BACKEND_URL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    }
  }
}
