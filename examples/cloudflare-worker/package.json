{
  "name": "workflow-cloudflare-worker-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "portless run --force --name cf-worker.hylo node scripts/dev.mjs",
    "dev:local": "HYLO_BACKEND_URL=http://localhost:8787 node scripts/dev.mjs",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@workflow/core": "workspace:*",
    "@workflow/server": "workspace:*",
    "hono": "^4.12.14",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@types/node": "^24.12.2",
    "typescript": "~6.0.2",
    "wrangler": "^4.0.0"
  }
}
